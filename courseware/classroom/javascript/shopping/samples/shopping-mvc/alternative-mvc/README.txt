Alternative version, based on code in keyup.

- Removes Model.items, the items are passed to View.update() as a parameter.

- Adds a isValid method to the controller so business logic is there

- Multiple Views -- a view that shows the list in the console.

- A FatController -- that contains the Model